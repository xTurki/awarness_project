# Secrets — never commit. .env.example is committed instead.
.env
*.env
!.env.example

# Local notes holding the Gmail address and its App Password.
Gmailemail.txt

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# Uploaded content (instructor images, Phase 1). Lives in a Docker volume, not here.
backend/app/uploads/

# iCloud and OS clutter — this project lives in an iCloud folder
*.icloud
.DS_Store
Thumbs.db
desktop.ini
Icon?

# Editors
.idea/
.vscode/
*.swp
*~
